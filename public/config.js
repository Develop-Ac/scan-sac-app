// Configuracao de ambiente do app de scan.
// apiBase = URL do backend (escaner-fiscal-backend). Ex.: "https://escaner-api.intranet.local"
// Deixe "" para usar a mesma origem (util em dev com proxy). NAO deve terminar com "/".
window.APP_CONFIG = {
  apiBase: '',
};
